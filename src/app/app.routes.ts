import { Routes } from '@angular/router';
import { CalculateAmmountComponent } from './calculate-ammount/calculate-ammount.component';

export const routes: Routes = [
    {
        path:'',
        component:CalculateAmmountComponent
    }
];
